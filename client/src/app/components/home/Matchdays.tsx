import { Box, Typography } from '@mui/material'

const Matchdays = () => {
    return (
        <Box sx={{
            height: '100vh'
        }}>
            <Typography>Matchdays</Typography>
        </Box>
    )
}

export default Matchdays