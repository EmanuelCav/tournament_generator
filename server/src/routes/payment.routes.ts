import { Router } from 'express';

const router = Router()

router.post('/payments', )
router.get('/payments/success')
router.get('/payments/failure')
router.get('/payments/pending')

export default router